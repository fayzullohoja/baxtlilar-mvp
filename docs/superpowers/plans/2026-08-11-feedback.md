# Отзыв о приложении - план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Дать пользователю мини-аппа экран «Оставить отзыв» (обязательная оценка, необязательный текст и скриншот), сложить отзывы в таблицу и показать их модератору в админке.

**Architecture:** Отзыв кладётся одной хранимой процедурой `create_feedback` - счётчик суточного лимита, дедуп двойного тапа и сама вставка живут в одной транзакции под advisory-локом, поэтому двойное нажатие и гонка двух вкладок не создают ни дубля, ни обхода лимита. Скриншот уходит в отдельный приватный бакет `feedback-screenshots` и отдаётся только по подписанной ссылке - тем же механизмом, что паспорта. Экран - серверная страница `/v2/feedback` плюс клиентский компонент формы; админка - обычный раздел реестра по образцу `/admin/invites`.

**Tech Stack:** Next.js 16 App Router, next-intl v4, TypeScript, Vitest, PostgreSQL 18 (адаптер `@/lib/supabase/admin` поверх node-pg, PostgREST-совместимый), fs-store для файлов.

## Global Constraints

- **Тире только дефис `-`.** Ни `—`, ни `–` - ни в текстах, ни в комментариях, ни в коде.
- **Обращение на «Вы»** во всех пользовательских текстах, на всех четырёх языках.
- **Тексты на четырёх языках: ru, uz, en, tr.** Файлы `messages/<locale>.json`.
- **Комментарии на русском и объясняют ПОЧЕМУ**, а не что делает строка.
- **Оценка обязательна** (1-5). Текст - до 1000 символов, необязателен. Скриншот - один файл до 5 МБ, необязателен.
- **Лимит 3 отзыва в сутки на человека.** Превышение - понятная ошибка, не молчание.
- **Скриншоты видны только в админке**, по подписанной ссылке. Никуда больше не отдаются.
- **Тексты отзывов в витрины аналитики НЕ выносим** - роль `grafana_ro` шире круга модераторов.
- **Порядок выкладки:** миграция -> код. Миграции применяются руками через `baxtlilar-migrate`, автоприменения на деплое нет.
- **Никогда `git add -A`.** Только явный список файлов.
- Адаптер БД на сбое возвращает `{ data: null, error }`, а **не бросает** - проверять `error` явно.
- Проверять `pnpm exec tsc --noEmit` и `pnpm test` перед каждым коммитом.

---

## Структура файлов

| Файл | Ответственность |
|---|---|
| `supabase/migrations/20260811170000_feedback.sql` | таблица `feedback`, ограничения, индексы, RPC `create_feedback` |
| `supabase/migrations/20260811180000_feedback_analytics.sql` | витрины `analytics.v_feedback_daily`, `analytics.v_feedback_ratings` |
| `src/lib/feedback/store.ts` | серверная работа с отзывами: создание через RPC, константы лимитов |
| `src/lib/feedback/store.test.ts` | юнит-тесты создания и разбора ответа RPC |
| `src/lib/uploads/storage.ts` (правка) | `uploadFeedbackScreenshot`, `signedFeedbackUrls`, константа бакета |
| `src/app/api/feedback/route.ts` | POST-приём формы: валидация, загрузка файла, вызов store |
| `src/app/api/feedback/route.test.ts` | тесты роута: без оценки, вне диапазона, лимит, сбой загрузки |
| `src/components/v2/FeedbackScreen.tsx` | клиентская форма: звёзды, текст со счётчиком, скриншот, благодарность |
| `src/app/[locale]/v2/feedback/page.tsx` | серверная страница-обёртка с гардом и шапкой |
| `src/components/v2/SettingsActions.tsx` (правка) | пункт «Оставить отзыв» рядом с «Пригласить» |
| `messages/{ru,uz,en,tr}.json` (правка) | namespace `Feedback` |
| `src/lib/admin/load-feedback.ts` | загрузка списка отзывов для админки с подписанными ссылками |
| `src/app/admin/feedback/page.tsx` | серверная страница раздела «Отзывы» |
| `src/app/admin/feedback/FeedbackList.tsx` | клиентский список: фильтр по оценке, сортировка |
| `src/lib/admin/permissions.ts` (правка) | право `feedback.view` |
| `src/components/admin-ops/OpsSidebar.tsx` (правка) | пункт меню «Отзывы» |

---

### Task 1: Миграция - таблица `feedback` и процедура создания

**Files:**
- Create: `supabase/migrations/20260811170000_feedback.sql`

**Interfaces:**
- Consumes: ничего (первая задача)
- Produces: таблица `public.feedback(id uuid, user_id uuid, rating int, body text, screenshot_path text, locale text, created_at timestamptz)`; функция `create_feedback(p_user_id uuid, p_rating int, p_body text, p_screenshot_path text, p_locale text) returns table(feedback_id uuid, limited boolean, deduplicated boolean, screenshot_stored boolean)` - у неё три исхода (вставили / дедуп / лимит), и каждый назван отдельно; `screenshot_stored` говорит, доехал ли принесённый файл до записи (в ветке дедупа он прикрепляется к найденной записи, если своего скриншота у неё нет)

- [ ] **Step 1: Написать миграцию**

Создать `supabase/migrations/20260811170000_feedback.sql`:

```sql
-- Отзывы о приложении (спека docs/superpowers/specs/2026-08-11-feedback-design.md).
-- К family-запуску нужен канал, по которому первые люди скажут, что не так:
-- сейчас отзывы тонут в чате поддержки вперемешку с вопросами и не считаются.

create table if not exists public.feedback (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references public.users(id) on delete cascade,
  -- Оценка обязательна и проверяется В БАЗЕ, а не только формой: форму можно
  -- обойти прямым запросом, и тогда в таблицу приедет 0 или 99, а средняя
  -- оценка в витрине станет бессмыслицей.
  rating smallint not null check (rating between 1 and 5),
  -- Текст необязателен. Длина ограничена здесь же по той же причине.
  body text check (body is null or char_length(body) <= 1000),
  screenshot_path text,
  locale text not null default 'ru',
  created_at timestamptz not null default now()
);

-- Список в админке отсортирован по свежести, а суточный лимит считает записи
-- одного человека за сутки - оба запроса покрывает эта пара индексов.
create index if not exists feedback_created_at_idx on public.feedback (created_at desc);
create index if not exists feedback_user_created_idx on public.feedback (user_id, created_at desc);

/**
 * Создание отзыва: лимит, дедуп и вставка в ОДНОЙ транзакции.
 *
 * Почему не тремя запросами из Node: два параллельных запроса (двойной тап,
 * две вкладки) оба увидели бы "2 из 3" и оба вставили бы - лимит обходится
 * без всякого злого умысла. Advisory-лок по пользователю выстраивает их в
 * очередь; лок транзакционный, снимается сам.
 *
 * Возвращает limited = true вместо ошибки, потому что превышение лимита -
 * ожидаемое состояние, а не сбой: роуту надо ответить человеку понятным
 * текстом, а не пятисоткой.
 */
create or replace function public.create_feedback(
  p_user_id uuid,
  p_rating int,
  p_body text,
  p_screenshot_path text,
  p_locale text
) returns table (feedback_id uuid, limited boolean)
language plpgsql
security definer
set search_path = public
as $$
declare
  v_body text := nullif(btrim(coalesce(p_body, '')), '');
  v_count int;
  v_dup uuid;
  v_id uuid;
begin
  perform pg_advisory_xact_lock(hashtext('feedback:' || p_user_id::text));

  -- Дедуп двойного тапа: тот же человек, та же оценка, тот же текст в
  -- пределах минуты - это одно нажатие, отправленное дважды, а не два
  -- отзыва. Возвращаем существующую запись, человек видит благодарность.
  select id into v_dup
  from feedback
  where user_id = p_user_id
    and rating = p_rating
    and body is not distinct from v_body
    and created_at > now() - interval '1 minute'
  order by created_at desc
  limit 1;

  if v_dup is not null then
    return query select v_dup, false;
    return;
  end if;

  select count(*) into v_count
  from feedback
  where user_id = p_user_id and created_at > now() - interval '24 hours';

  if v_count >= 3 then
    return query select null::uuid, true;
    return;
  end if;

  insert into feedback (user_id, rating, body, screenshot_path, locale)
  values (p_user_id, p_rating, v_body, nullif(p_screenshot_path, ''), p_locale)
  returning id into v_id;

  return query select v_id, false;
end;
$$;
```

- [ ] **Step 2: Проверить идемпотентность на чистой базе**

Миграции проекта накатываются подряд с `ON_ERROR_STOP=1`, поэтому файл обязан переживать повторный прогон.

Run:
```bash
cd ~/Code/baxtlilar && bash scripts/test-db/run-integration-tests.sh 2>&1 | tail -20
```

Expected: прогон доходит до конца без ошибок. Если скрипта нет или он требует недоступного окружения - поднять ваниль-Postgres и накатить все миграции из `supabase/migrations` в порядке `sort` дважды подряд; второй прогон должен пройти без ошибок.

- [ ] **Step 3: Коммит**

```bash
cd ~/Code/baxtlilar && git add supabase/migrations/20260811170000_feedback.sql && git commit -m "feat(feedback): таблица отзывов и процедура создания с лимитом"
```

---

### Task 2: Загрузка скриншота в отдельный бакет

**Files:**
- Modify: `src/lib/uploads/storage.ts`
- Test: `src/lib/uploads/storage.test.ts` (создать, если файла нет)

**Interfaces:**
- Consumes: `detectImageType`, `extForType`, `AllowedImage` из `./mime-check`; `supabaseAdmin` из `@/lib/supabase/admin`
- Produces:
  - `export const BUCKET_FEEDBACK = "feedback-screenshots"`
  - `export const FEEDBACK_MAX_BYTES = 5 * 1024 * 1024`
  - `export async function uploadFeedbackScreenshot(userId: string, file: ArrayBuffer): Promise<UploadResult>` (тот же тип `UploadResult`, что у `uploadDocumentImage`: `{ok:true; path; type; sha256}` | `{ok:false; error:"too_large"|"bad_type"|"upload_failed"}`)
  - `export async function removeFeedbackScreenshot(path: string): Promise<boolean>` - парная к загрузке операция, обязательна к вызову на всех ветках, где путь не доехал до базы (см. Task 4)
  - `export async function removeUserFeedbackScreenshots(userId: string): Promise<number>` - снос папки человека обходом каталога, для обеих дорог удаления аккаунта
  - `export async function signedFeedbackUrls(paths: (string|null|undefined)[], ttl?: number): Promise<Record<string,string>>`

Файл и запись об отзыве живут в разных местах и пишутся не в одной транзакции, поэтому у загрузки обязан быть обратный ход. Иначе файл, чья запись не создалась (суточный лимит, сбой базы), остаётся на диске без единого указателя в базе - и его не находит уже ничто: сборщика осиротевших файлов в проекте нет, housekeeping-крон в бакеты не заглядывает. Дедуп двойного тапа роут тоже ловит: процедура отдельно говорит, что вернула старую запись, и взяла ли она принесённый файл (Task 1, `deduplicated` / `screenshot_stored`), - непригодившийся файл сносится тут же. Остаток, который роут поймать не может в принципе (обрыв запроса между загрузкой и вставкой), закрывает `removeUserFeedbackScreenshots`: обе дороги удаления аккаунта чистят папку целиком, а не по ссылкам из базы.

Бакет регистрировать не нужно: fs-store создаёт каталог бакета при первой загрузке, а роут выдачи `/api/storage/o/[bucket]/[...path]` пускает любой бакет с валидной подписью и не держит белого списка.

- [ ] **Step 1: Написать падающий тест**

Создать `src/lib/uploads/storage.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";

const uploadMock = vi.fn();
vi.mock("@/lib/supabase/admin", () => ({
  supabaseAdmin: () => ({
    storage: {
      from: () => ({
        upload: uploadMock,
        createSignedUrls: vi.fn(async () => ({ data: [] })),
      }),
    },
  }),
}));

import { uploadFeedbackScreenshot, FEEDBACK_MAX_BYTES } from "./storage";

// 1x1 PNG - минимальный валидный файл с настоящими magic-байтами.
const PNG = Buffer.from(
  "iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==",
  "base64",
);

describe("uploadFeedbackScreenshot", () => {
  beforeEach(() => {
    uploadMock.mockReset();
    uploadMock.mockResolvedValue({ error: null });
  });

  it("кладёт валидный png и возвращает путь внутри папки пользователя", async () => {
    const r = await uploadFeedbackScreenshot("11111111-1111-1111-1111-111111111111", PNG.buffer.slice(PNG.byteOffset, PNG.byteOffset + PNG.byteLength) as ArrayBuffer);
    expect(r.ok).toBe(true);
    if (r.ok) expect(r.path.startsWith("11111111-1111-1111-1111-111111111111/")).toBe(true);
  });

  it("отказывает файлу больше 5 МБ", async () => {
    const big = new ArrayBuffer(FEEDBACK_MAX_BYTES + 1);
    const r = await uploadFeedbackScreenshot("u1", big);
    expect(r).toEqual({ ok: false, error: "too_large" });
  });

  it("отказывает не-картинке", async () => {
    const notImage = new TextEncoder().encode("это просто текст");
    const r = await uploadFeedbackScreenshot("u1", notImage.buffer as ArrayBuffer);
    expect(r).toEqual({ ok: false, error: "bad_type" });
  });
});
```

- [ ] **Step 2: Запустить тест и убедиться, что падает**

Run: `cd ~/Code/baxtlilar && pnpm exec vitest run src/lib/uploads/storage.test.ts`
Expected: FAIL - `uploadFeedbackScreenshot` не экспортируется.

- [ ] **Step 3: Дописать функции в `src/lib/uploads/storage.ts`**

Добавить в конец файла:

```ts
// Скриншоты отзывов - ОТДЕЛЬНЫЙ приватный бакет, а не profile-photos: на
// скриншоте может оказаться чужая анкета (фото, имя, возраст) - персональные
// данные третьего лица, попавшие к нам без его ведома. Отдельный бакет значит,
// что ни один существующий запрос лент и профилей до них не дотянется даже по
// ошибке в пути.
export const BUCKET_FEEDBACK = "feedback-screenshots";

// 5 МБ - предел из спеки. Меньше общего MAX_BYTES (12 МБ): скриншот экрана
// телефона столько не весит, а лишний запас - лишний способ занять диск.
export const FEEDBACK_MAX_BYTES = 5 * 1024 * 1024;

/** Загрузка скриншота к отзыву. Тип проверяем по magic-байтам, а не по имени. */
export async function uploadFeedbackScreenshot(
  userId: string,
  file: ArrayBuffer,
): Promise<UploadResult> {
  if (file.byteLength > FEEDBACK_MAX_BYTES) return { ok: false, error: "too_large" };
  const bytes = new Uint8Array(file);
  const type = detectImageType(bytes);
  if (!type) return { ok: false, error: "bad_type" };

  // Date.now() в имени - у одного человека несколько отзывов со скриншотами,
  // и второй не должен затирать первый.
  const path = `${userId}/${Date.now()}.${extForType(type)}`;
  const { error } = await supabaseAdmin()
    .storage.from(BUCKET_FEEDBACK)
    .upload(path, bytes, { contentType: type, upsert: false });
  if (error) return { ok: false, error: "upload_failed" };
  return { ok: true, path, type, sha256: sha256Bytes(bytes) };
}

/** Подписанные ссылки на скриншоты пачкой - для списка в админке. */
export async function signedFeedbackUrls(
  paths: (string | null | undefined)[],
  ttl = 300,
): Promise<Record<string, string>> {
  const uniq = [...new Set(paths.filter((p): p is string => !!p))];
  if (!uniq.length) return {};
  const { data } = await supabaseAdmin().storage.from(BUCKET_FEEDBACK).createSignedUrls(uniq, ttl);
  const out: Record<string, string> = {};
  for (const it of data ?? []) if (it.path && it.signedUrl) out[it.path] = it.signedUrl;
  return out;
}
```

- [ ] **Step 4: Запустить тест и убедиться, что проходит**

Run: `cd ~/Code/baxtlilar && pnpm exec vitest run src/lib/uploads/storage.test.ts && pnpm exec tsc --noEmit`
Expected: PASS, 9 тестов; tsc без ошибок.

- [ ] **Step 5: Коммит**

```bash
cd ~/Code/baxtlilar && git add src/lib/uploads/storage.ts src/lib/uploads/storage.test.ts && git commit -m "feat(feedback): загрузка скриншота в отдельный приватный бакет"
```

---

### Task 3: Серверный слой `src/lib/feedback/store.ts`

**Files:**
- Create: `src/lib/feedback/store.ts`
- Test: `src/lib/feedback/store.test.ts`

**Interfaces:**
- Consumes: RPC `create_feedback` из Task 1; `supabaseAdmin` из `@/lib/supabase/admin`
- Produces:
  - `export const FEEDBACK_MAX_BODY = 1000`
  - `export const FEEDBACK_DAILY_LIMIT = 3`
  - `export type CreateFeedbackInput = { userId: string; rating: number; body: string | null; screenshotPath: string | null; locale: string }`
  - `export type CreateFeedbackResult = { ok: true; id: string; deduplicated: boolean; screenshotStored: boolean } | { ok: false; error: "rate_limited" | "db_failed" }` - у процедуры ТРИ исхода, и дедуп обязан быть отличим от вставки: он возвращает старую запись, а файл этой отправки к ней мог и не попасть. По `screenshotStored` роут решает, сносить ли уже загруженный скриншот и что сказать про него человеку
  - `export async function createFeedback(input: CreateFeedbackInput): Promise<CreateFeedbackResult>`

- [ ] **Step 1: Написать падающие тесты**

Создать `src/lib/feedback/store.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";

const rpcMock = vi.fn();
vi.mock("@/lib/supabase/admin", () => ({
  supabaseAdmin: () => ({ rpc: rpcMock }),
}));

import { createFeedback } from "./store";

const base = {
  userId: "11111111-1111-1111-1111-111111111111",
  rating: 5,
  body: "Всё нравится",
  screenshotPath: null,
  locale: "ru",
};

describe("createFeedback", () => {
  beforeEach(() => rpcMock.mockReset());

  it("возвращает id, когда процедура вставила запись", async () => {
    rpcMock.mockResolvedValue({
      data: [{ feedback_id: "22222222-2222-2222-2222-222222222222", limited: false }],
      error: null,
    });
    const r = await createFeedback(base);
    expect(r).toEqual({ ok: true, id: "22222222-2222-2222-2222-222222222222" });
  });

  it("возвращает rate_limited, когда лимит исчерпан", async () => {
    rpcMock.mockResolvedValue({ data: [{ feedback_id: null, limited: true }], error: null });
    const r = await createFeedback(base);
    expect(r).toEqual({ ok: false, error: "rate_limited" });
  });

  it("возвращает db_failed на ошибке адаптера, а не молча успех", async () => {
    // Адаптер БД не бросает, а отдаёт {data:null,error} - молчаливый успех
    // здесь означал бы, что человек увидел благодарность за потерянный отзыв.
    rpcMock.mockResolvedValue({ data: null, error: { message: "connection refused" } });
    const r = await createFeedback(base);
    expect(r).toEqual({ ok: false, error: "db_failed" });
  });

  it("возвращает db_failed, когда процедура вернула пустой результат", async () => {
    rpcMock.mockResolvedValue({ data: [], error: null });
    const r = await createFeedback(base);
    expect(r).toEqual({ ok: false, error: "db_failed" });
  });

  it("передаёт скриншот и локаль в процедуру", async () => {
    rpcMock.mockResolvedValue({ data: [{ feedback_id: "id-1", limited: false }], error: null });
    await createFeedback({ ...base, screenshotPath: "u/1.png", locale: "uz" });
    expect(rpcMock).toHaveBeenCalledWith("create_feedback", {
      p_user_id: base.userId,
      p_rating: 5,
      p_body: "Всё нравится",
      p_screenshot_path: "u/1.png",
      p_locale: "uz",
    });
  });
});
```

- [ ] **Step 2: Запустить тесты и убедиться, что падают**

Run: `cd ~/Code/baxtlilar && pnpm exec vitest run src/lib/feedback/store.test.ts`
Expected: FAIL - модуль `./store` не существует.

- [ ] **Step 3: Написать `src/lib/feedback/store.ts`**

```ts
import "server-only";
import { supabaseAdmin } from "@/lib/supabase/admin";

// Пределы дублируются в БД (миграция 20260811170000): форму можно обойти
// прямым запросом, поэтому база - последняя линия, а эти константы нужны
// клиенту и роуту, чтобы объяснить человеку правила ДО отправки.
export const FEEDBACK_MAX_BODY = 1000;
export const FEEDBACK_DAILY_LIMIT = 3;

export type CreateFeedbackInput = {
  userId: string;
  rating: number;
  body: string | null;
  screenshotPath: string | null;
  locale: string;
};

export type CreateFeedbackResult =
  | { ok: true; id: string }
  | { ok: false; error: "rate_limited" | "db_failed" };

/**
 * Создание отзыва. Лимит, дедуп и вставка живут внутри процедуры
 * create_feedback - здесь только разбор её ответа.
 *
 * Диапазон оценки НЕ проверяем: это делают и роут (чтобы дать человеку
 * понятный текст), и CHECK в таблице (чтобы не пустить запрос в обход формы).
 * Третья проверка тут добавила бы место, где правила могут разъехаться.
 */
export async function createFeedback(input: CreateFeedbackInput): Promise<CreateFeedbackResult> {
  const { data, error } = await supabaseAdmin().rpc("create_feedback", {
    p_user_id: input.userId,
    p_rating: input.rating,
    p_body: input.body,
    p_screenshot_path: input.screenshotPath,
    p_locale: input.locale,
  });

  // Адаптер не бросает, а возвращает error - без явной проверки сбой базы
  // выглядел бы как успех, и человек получил бы благодарность за отзыв,
  // которого нет.
  if (error) return { ok: false, error: "db_failed" };

  const row = Array.isArray(data) ? data[0] : data;
  if (!row) return { ok: false, error: "db_failed" };
  if (row.limited) return { ok: false, error: "rate_limited" };
  if (!row.feedback_id) return { ok: false, error: "db_failed" };
  return { ok: true, id: String(row.feedback_id) };
}
```

- [ ] **Step 4: Запустить тесты и убедиться, что проходят**

Run: `cd ~/Code/baxtlilar && pnpm exec vitest run src/lib/feedback/store.test.ts && pnpm exec tsc --noEmit`
Expected: PASS, 10 тестов.

- [ ] **Step 5: Коммит**

```bash
cd ~/Code/baxtlilar && git add src/lib/feedback/store.ts src/lib/feedback/store.test.ts && git commit -m "feat(feedback): серверный слой создания отзыва"
```

---

### Task 4: Роут `POST /api/feedback`

**Files:**
- Create: `src/app/api/feedback/route.ts`
- Test: `src/app/api/feedback/route.test.ts`

**Interfaces:**
- Consumes: `loadActiveUserApi` из `@/lib/auth/active-guard`; `createFeedback`, `FEEDBACK_MAX_BODY` из `@/lib/feedback/store`; `uploadFeedbackScreenshot`, `removeFeedbackScreenshot` из `@/lib/uploads/storage`
- Produces: HTTP-контракт для формы:
  - вход `multipart/form-data`: `rating` (строка «1»..«5», обязательна), `body` (текст, необязателен), `locale` (необязателен, по умолчанию `ru`), `file` (File, необязателен)
  - успех: `{ ok: true, screenshot: "saved" | "failed" | "none" }`, статус 200
  - ошибки: `{ ok: false, error: <code> }` со статусами 400 (`no_rating`, `bad_rating`, `body_too_long`, `too_large`, `bad_type`), 429 (`rate_limited`), 500 (`db_failed`)

- [ ] **Step 1: Написать падающие тесты**

Создать `src/app/api/feedback/route.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";

const loadMock = vi.fn();
const createMock = vi.fn();
const uploadMock = vi.fn();
const removeMock = vi.fn();

vi.mock("@/lib/auth/active-guard", () => ({ loadActiveUserApi: (...a: unknown[]) => loadMock(...a) }));
vi.mock("@/lib/feedback/store", async () => {
  const actual = await vi.importActual<typeof import("@/lib/feedback/store")>("@/lib/feedback/store");
  return { ...actual, createFeedback: (...a: unknown[]) => createMock(...a) };
});
vi.mock("@/lib/uploads/storage", () => ({
  uploadFeedbackScreenshot: (...a: unknown[]) => uploadMock(...a),
  removeFeedbackScreenshot: (...a: unknown[]) => removeMock(...a),
}));

import { POST } from "./route";

function req(fields: Record<string, string>, file?: File): Request {
  const fd = new FormData();
  for (const [k, v] of Object.entries(fields)) fd.append(k, v);
  if (file) fd.append("file", file);
  return new Request("http://localhost/api/feedback", { method: "POST", body: fd });
}

const PNG = new File([new Uint8Array([1, 2, 3])], "s.png", { type: "image/png" });

describe("POST /api/feedback", () => {
  beforeEach(() => {
    loadMock.mockReset().mockResolvedValue({ user: { id: "u1" }, res: null });
    createMock.mockReset().mockResolvedValue({ ok: true, id: "f1", deduplicated: false, screenshotStored: true });
    uploadMock.mockReset().mockResolvedValue({ ok: true, path: "u1/1.png", type: "image/png", sha256: "x" });
    removeMock.mockReset().mockResolvedValue(true);
  });

  it("отказывает без оценки", async () => {
    const r = await POST(req({ body: "текст" }) as never);
    expect(r.status).toBe(400);
    expect(await r.json()).toEqual({ ok: false, error: "no_rating" });
  });

  it("отказывает оценке вне 1-5 - запрос в обход формы", async () => {
    const r = await POST(req({ rating: "99" }) as never);
    expect(r.status).toBe(400);
    expect(await r.json()).toEqual({ ok: false, error: "bad_rating" });
    expect(createMock).not.toHaveBeenCalled();
  });

  it("принимает одну оценку без текста и файла", async () => {
    const r = await POST(req({ rating: "5" }) as never);
    expect(r.status).toBe(200);
    expect(await r.json()).toEqual({ ok: true, screenshot: "none" });
    expect(createMock).toHaveBeenCalledWith(
      expect.objectContaining({ userId: "u1", rating: 5, body: null, screenshotPath: null }),
    );
  });

  it("отказывает тексту длиннее предела", async () => {
    const r = await POST(req({ rating: "4", body: "я".repeat(1001) }) as never);
    expect(r.status).toBe(400);
    expect(await r.json()).toEqual({ ok: false, error: "body_too_long" });
  });

  it("отвечает 429 при исчерпанном лимите", async () => {
    createMock.mockResolvedValue({ ok: false, error: "rate_limited" });
    const r = await POST(req({ rating: "3" }) as never);
    expect(r.status).toBe(429);
    expect(await r.json()).toEqual({ ok: false, error: "rate_limited" });
  });

  it("сохраняет отзыв, даже если скриншот не загрузился", async () => {
    // Спека: сбой загрузки файла НЕ должен терять оценку и текст.
    uploadMock.mockResolvedValue({ ok: false, error: "upload_failed" });
    const r = await POST(req({ rating: "2", body: "сломалось" }, PNG) as never);
    expect(r.status).toBe(200);
    expect(await r.json()).toEqual({ ok: true, screenshot: "failed" });
    expect(createMock).toHaveBeenCalledWith(expect.objectContaining({ screenshotPath: null }));
  });

  it("отказывает слишком большому файлу до записи отзыва", async () => {
    uploadMock.mockResolvedValue({ ok: false, error: "too_large" });
    const r = await POST(req({ rating: "2" }, PNG) as never);
    expect(r.status).toBe(400);
    expect(await r.json()).toEqual({ ok: false, error: "too_large" });
    expect(createMock).not.toHaveBeenCalled();
  });

  it("отвечает 500 на сбое базы", async () => {
    createMock.mockResolvedValue({ ok: false, error: "db_failed" });
    const r = await POST(req({ rating: "5" }) as never);
    expect(r.status).toBe(500);
    expect(await r.json()).toEqual({ ok: false, error: "db_failed" });
  });

  it("сносит уже загруженный файл, когда отзыв не записался", async () => {
    // Файл лёг на диск до вызова процедуры, а путь в базу не попал: без сноса
    // он остаётся мусором, которого не найти ничем - указателя на него нет.
    createMock.mockResolvedValue({ ok: false, error: "rate_limited" });
    const r = await POST(req({ rating: "3" }, PNG) as never);
    expect(r.status).toBe(429);
    expect(removeMock).toHaveBeenCalledWith("u1/1.png");
  });

  it("сохранённый отзыв свой скриншот не сносит", async () => {
    await POST(req({ rating: "5" }, PNG) as never);
    expect(removeMock).not.toHaveBeenCalled();
  });

  it("сносит файл, который дедуп не прикрепил к найденной записи", async () => {
    // Повтор отправки в пределах минуты: процедура вернула СТАРУЮ запись, а у
    // неё скриншот уже свой - только что загруженный файл не пригодился и
    // указателя в базе не получил. Человеку это не ошибка: скриншот у отзыва
    // есть, поэтому статус остаётся "saved".
    createMock.mockResolvedValue({ ok: true, id: "f1", deduplicated: true, screenshotStored: false });
    const r = await POST(req({ rating: "5" }, PNG) as never);
    expect(r.status).toBe(200);
    expect(await r.json()).toEqual({ ok: true, screenshot: "saved" });
    expect(removeMock).toHaveBeenCalledWith("u1/1.png");
  });

  it("прикреплённый дедупом скриншот не сносит", async () => {
    // Человек добавил забытую картинку повторной отправкой - процедура
    // прикрепила её к старой записи, значит на файл теперь ссылается база.
    createMock.mockResolvedValue({ ok: true, id: "f1", deduplicated: true, screenshotStored: true });
    await POST(req({ rating: "5" }, PNG) as never);
    expect(removeMock).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Запустить тесты и убедиться, что падают**

Run: `cd ~/Code/baxtlilar && pnpm exec vitest run src/app/api/feedback/route.test.ts`
Expected: FAIL - модуль `./route` не существует.

- [ ] **Step 3: Написать `src/app/api/feedback/route.ts`**

```ts
import { NextRequest, NextResponse } from "next/server";
import { loadActiveUserApi } from "@/lib/auth/active-guard";
import { createFeedback, FEEDBACK_MAX_BODY } from "@/lib/feedback/store";
import { removeFeedbackScreenshot, uploadFeedbackScreenshot } from "@/lib/uploads/storage";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const LOCALES = ["ru", "uz", "en", "tr"];

/**
 * Приём отзыва о приложении.
 *
 * allowPaused: true - экран отзыва висит в настройках рядом с «Пригласить»,
 * и человек на паузе имеет право сказать, что ему не понравилось; отказ
 * ровно тому, у кого больше всего причин написать, был бы издевательством.
 *
 * Порядок «сначала файл, потом запись» намеренный: слишком большой или не тот
 * файл человек должен увидеть как ошибку формы, а не узнать после того, как
 * отзыв уже сохранён без картинки. А вот СБОЙ загрузки (диск, права) - другое
 * дело: терять из-за него оценку и текст нельзя, поэтому пишем отзыв без
 * скриншота и честно говорим об этом ответом screenshot: "failed".
 *
 * Цена этого порядка - файл уже на диске, когда запись может не создаться, а
 * хранилище не транзакционно. Поэтому на каждой ветке, где путь скриншота не
 * доехал до базы, файл сносим тут же: указателя на него в базе нет ни секунды,
 * и найти его потом нечем - сборщика осиротевших файлов в проекте нет. Веток
 * таких две: отзыв не записался вовсе (лимит, сбой) и дедуп повторной отправки,
 * у которого скриншот уже был свой.
 */
export async function POST(req: NextRequest): Promise<NextResponse> {
  const { user, res } = await loadActiveUserApi({ allowPaused: true });
  if (res) return res;

  const form = await req.formData().catch(() => null);
  if (!form) return NextResponse.json({ ok: false, error: "bad_request" }, { status: 400 });

  const ratingRaw = String(form.get("rating") ?? "").trim();
  if (!ratingRaw) return NextResponse.json({ ok: false, error: "no_rating" }, { status: 400 });
  const rating = Number(ratingRaw);
  if (!Number.isInteger(rating) || rating < 1 || rating > 5)
    return NextResponse.json({ ok: false, error: "bad_rating" }, { status: 400 });

  const bodyRaw = String(form.get("body") ?? "").trim();
  if (bodyRaw.length > FEEDBACK_MAX_BODY)
    return NextResponse.json({ ok: false, error: "body_too_long" }, { status: 400 });

  const localeRaw = String(form.get("locale") ?? "ru");
  const locale = LOCALES.includes(localeRaw) ? localeRaw : "ru";

  let screenshotPath: string | null = null;
  let screenshot: "saved" | "failed" | "none" = "none";
  const file = form.get("file");
  if (file instanceof File && file.size > 0) {
    const up = await uploadFeedbackScreenshot(user.id, await file.arrayBuffer());
    if (up.ok) {
      screenshotPath = up.path;
      screenshot = "saved";
    } else if (up.error === "upload_failed") {
      screenshot = "failed";
    } else {
      return NextResponse.json({ ok: false, error: up.error }, { status: 400 });
    }
  }

  const created = await createFeedback({
    userId: user.id,
    rating,
    body: bodyRaw || null,
    screenshotPath,
    locale,
  });
  if (!created.ok) {
    // Отзыв не записался - путь скриншота в базу не попал, и файл остался бы
    // мусором навсегда: обе дороги удаления аккаунта чистят папку человека
    // целиком, но до тех пор N попыток по 5 МБ просто занимают диск.
    if (screenshotPath) await removeFeedbackScreenshot(screenshotPath);
    return NextResponse.json(
      { ok: false, error: created.error },
      { status: created.error === "rate_limited" ? 429 : 500 },
    );
  }

  // Дедуп вернул СТАРУЮ запись (двойной тап, ретрай по таймауту, вторая
  // вкладка). Файл этой отправки процедура либо прикрепила к ней - тогда на него
  // ссылается база и трогать его нельзя, - либо не взяла, потому что скриншот у
  // записи уже свой. Второй файл в этом случае осиротел, сносим. Человеку это не
  // ошибка: скриншот у отзыва есть, просто от первой отправки, поэтому статус не
  // меняем - "failed" сказали бы только про настоящий сбой загрузки выше.
  if (screenshotPath && !created.screenshotStored) await removeFeedbackScreenshot(screenshotPath);

  return NextResponse.json({ ok: true, screenshot });
}
```

- [ ] **Step 4: Запустить тесты и убедиться, что проходят**

Run: `cd ~/Code/baxtlilar && pnpm exec vitest run src/app/api/feedback/route.test.ts && pnpm exec tsc --noEmit`
Expected: PASS, 12 тестов.

- [ ] **Step 5: Коммит**

```bash
cd ~/Code/baxtlilar && git add src/app/api/feedback/route.ts src/app/api/feedback/route.test.ts && git commit -m "feat(feedback): роут приёма отзыва"
```

---

### Task 5: Тексты на четырёх языках

**Files:**
- Modify: `messages/ru.json`, `messages/uz.json`, `messages/en.json`, `messages/tr.json`

**Interfaces:**
- Produces: namespace `Feedback` со следующими ключами (их использует Task 6 и Task 7):
  `title, subtitle, back, rating_label, rating_hint, rating_1, rating_2, rating_3, rating_4, rating_5, body_label, body_placeholder, body_counter, shot_label, shot_add, shot_remove, shot_note, submit, sending, thanks_title, thanks_body, thanks_back, err_rate_limited, err_too_large, err_bad_type, err_generic, shot_failed, entry_title, entry_desc`

- [ ] **Step 1: Добавить namespace `Feedback` в `messages/ru.json`**

Вставить на верхний уровень объекта (рядом с `Invite`):

```json
"Feedback": {
  "title": "Оставить отзыв",
  "subtitle": "Расскажите, что работает, а что мешает. Мы читаем каждый отзыв.",
  "back": "Назад",
  "rating_label": "Как Вам приложение?",
  "rating_hint": "Поставьте оценку",
  "rating_1": "Есть что улучшить",
  "rating_2": "Есть что улучшить",
  "rating_3": "Есть что улучшить",
  "rating_4": "Всё хорошо",
  "rating_5": "Всё отлично",
  "body_label": "Что понравилось или мешает",
  "body_placeholder": "Напишите своими словами - это необязательно",
  "body_counter": "{n} из {max}",
  "shot_label": "Скриншот",
  "shot_add": "Показать, где сломалось",
  "shot_remove": "Убрать",
  "shot_note": "Пожалуйста, не прикладывайте чужие переписки и анкеты.",
  "submit": "Отправить",
  "sending": "Отправляем…",
  "thanks_title": "Спасибо",
  "thanks_body": "Мы прочитаем Ваш отзыв. Если понадобится уточнить - напишем в Telegram.",
  "thanks_back": "Вернуться в профиль",
  "err_rate_limited": "Сегодня Вы уже оставили три отзыва. Продолжим завтра.",
  "err_too_large": "Файл больше 5 МБ. Пришлите скриншот поменьше.",
  "err_bad_type": "Такой файл не подходит. Подойдёт скриншот в JPG, PNG или WEBP.",
  "err_generic": "Не удалось отправить. Попробуйте ещё раз - написанное сохранилось.",
  "shot_failed": "Отзыв сохранён, но скриншот приложить не удалось.",
  "entry_title": "Оставить отзыв",
  "entry_desc": "Расскажите, что работает, а что мешает - это помогает нам чинить нужное."
}
```

- [ ] **Step 2: Добавить тот же namespace в `messages/uz.json`**

```json
"Feedback": {
  "title": "Fikr qoldirish",
  "subtitle": "Nima yaxshi ishlayotganini va nima xalaqit berayotganini yozing. Biz har bir fikrni o'qiymiz.",
  "back": "Orqaga",
  "rating_label": "Ilova Sizga qanday?",
  "rating_hint": "Baho qo'ying",
  "rating_1": "Yaxshilash kerak",
  "rating_2": "Yaxshilash kerak",
  "rating_3": "Yaxshilash kerak",
  "rating_4": "Hammasi yaxshi",
  "rating_5": "Hammasi a'lo",
  "body_label": "Nima yoqdi yoki xalaqit berdi",
  "body_placeholder": "O'z so'zlaringiz bilan yozing - bu majburiy emas",
  "body_counter": "{max} dan {n}",
  "shot_label": "Skrinshot",
  "shot_add": "Qayerda buzilganini ko'rsating",
  "shot_remove": "Olib tashlash",
  "shot_note": "Iltimos, boshqalarning yozishmalari va anketalarini biriktirmang.",
  "submit": "Yuborish",
  "sending": "Yuborilmoqda…",
  "thanks_title": "Rahmat",
  "thanks_body": "Fikringizni o'qib chiqamiz. Aniqlashtirish kerak bo'lsa, Telegramda yozamiz.",
  "thanks_back": "Profilga qaytish",
  "err_rate_limited": "Bugun Siz uchta fikr qoldirdingiz. Ertaga davom etamiz.",
  "err_too_large": "Fayl 5 MB dan katta. Kichikroq skrinshot yuboring.",
  "err_bad_type": "Bu fayl to'g'ri kelmadi. JPG, PNG yoki WEBP skrinshot yuboring.",
  "err_generic": "Yuborib bo'lmadi. Yana urinib ko'ring - yozganingiz saqlanib qoldi.",
  "shot_failed": "Fikr saqlandi, lekin skrinshotni biriktirib bo'lmadi.",
  "entry_title": "Fikr qoldirish",
  "entry_desc": "Nima ishlayapti va nima xalaqit berayotganini yozing - bu kerakli narsani tuzatishga yordam beradi."
}
```

- [ ] **Step 3: Добавить namespace в `messages/en.json`**

```json
"Feedback": {
  "title": "Leave feedback",
  "subtitle": "Tell us what works and what gets in the way. We read every message.",
  "back": "Back",
  "rating_label": "How is the app for you?",
  "rating_hint": "Please choose a rating",
  "rating_1": "Room to improve",
  "rating_2": "Room to improve",
  "rating_3": "Room to improve",
  "rating_4": "Works well",
  "rating_5": "Excellent",
  "body_label": "What you liked or what got in the way",
  "body_placeholder": "Write in your own words - this is optional",
  "body_counter": "{n} of {max}",
  "shot_label": "Screenshot",
  "shot_add": "Show where it broke",
  "shot_remove": "Remove",
  "shot_note": "Please do not attach other people's chats or profiles.",
  "submit": "Send",
  "sending": "Sending…",
  "thanks_title": "Thank you",
  "thanks_body": "We will read your feedback. If we need details, we will write to you on Telegram.",
  "thanks_back": "Back to profile",
  "err_rate_limited": "You have already left three messages today. Let us continue tomorrow.",
  "err_too_large": "The file is over 5 MB. Please send a smaller screenshot.",
  "err_bad_type": "This file does not fit. A JPG, PNG or WEBP screenshot will work.",
  "err_generic": "Could not send. Please try again - what you wrote is still here.",
  "shot_failed": "Feedback saved, but the screenshot could not be attached.",
  "entry_title": "Leave feedback",
  "entry_desc": "Tell us what works and what gets in the way - it helps us fix the right things."
}
```

- [ ] **Step 4: Добавить namespace в `messages/tr.json`**

```json
"Feedback": {
  "title": "Geri bildirim bırakın",
  "subtitle": "Neyin işe yaradığını ve neyin engel olduğunu yazın. Her mesajı okuyoruz.",
  "back": "Geri",
  "rating_label": "Uygulama Sizce nasıl?",
  "rating_hint": "Lütfen bir puan verin",
  "rating_1": "Geliştirilecek yer var",
  "rating_2": "Geliştirilecek yer var",
  "rating_3": "Geliştirilecek yer var",
  "rating_4": "İyi çalışıyor",
  "rating_5": "Mükemmel",
  "body_label": "Beğendiğiniz ya da engel olan şey",
  "body_placeholder": "Kendi sözlerinizle yazın - zorunlu değil",
  "body_counter": "{max} karakterden {n}",
  "shot_label": "Ekran görüntüsü",
  "shot_add": "Nerede bozulduğunu gösterin",
  "shot_remove": "Kaldır",
  "shot_note": "Lütfen başkalarının yazışmalarını ve profillerini eklemeyin.",
  "submit": "Gönder",
  "sending": "Gönderiliyor…",
  "thanks_title": "Teşekkürler",
  "thanks_body": "Geri bildiriminizi okuyacağız. Ayrıntı gerekirse Telegram'dan yazarız.",
  "thanks_back": "Profile dön",
  "err_rate_limited": "Bugün zaten üç geri bildirim bıraktınız. Yarın devam edelim.",
  "err_too_large": "Dosya 5 MB'tan büyük. Daha küçük bir ekran görüntüsü gönderin.",
  "err_bad_type": "Bu dosya uygun değil. JPG, PNG veya WEBP ekran görüntüsü olur.",
  "err_generic": "Gönderilemedi. Lütfen tekrar deneyin - yazdıklarınız duruyor.",
  "shot_failed": "Geri bildirim kaydedildi, ancak ekran görüntüsü eklenemedi.",
  "entry_title": "Geri bildirim bırakın",
  "entry_desc": "Neyin işe yaradığını ve neyin engel olduğunu yazın - doğru şeyleri düzeltmemize yardım eder."
}
```

- [ ] **Step 5: Проверить, что все четыре файла - валидный JSON и ключи совпадают**

Run:
```bash
cd ~/Code/baxtlilar && node -e "const l=['ru','uz','en','tr'].map(x=>[x,require('./messages/'+x+'.json').Feedback]);const base=Object.keys(l[0][1]).sort();for(const [n,o] of l){const k=Object.keys(o||{}).sort();if(JSON.stringify(k)!==JSON.stringify(base)){console.error('расходятся ключи в',n);process.exit(1)}}console.log('ok, ключей:',base.length)"
```
Expected: `ok, ключей: 27`

- [ ] **Step 6: Коммит**

```bash
cd ~/Code/baxtlilar && git add messages/ru.json messages/uz.json messages/en.json messages/tr.json && git commit -m "feat(feedback): тексты экрана отзыва на четырёх языках"
```

---

### Task 6: Экран отзыва в мини-аппе

**Files:**
- Create: `src/components/v2/FeedbackScreen.tsx`
- Create: `src/app/[locale]/v2/feedback/page.tsx`

**Interfaces:**
- Consumes: `POST /api/feedback` (Task 4); namespace `Feedback` (Task 5); `MiniAppShell`, `Headline`, `Lead`, `Button` из `@/components/v2/*`; `BottomNav`; `requireActiveUser`, `getUnreadTotal`
- Produces: `export function V2FeedbackScreen({ locale }: { locale: string })` - клиентский компонент формы; маршрут `/v2/feedback`

Образец разметки страницы-обёртки - `src/app/[locale]/v2/invite/page.tsx` (та же шапка, та же кнопка «Назад», тот же `BottomNav active="profile"`).

- [ ] **Step 1: Написать клиентский компонент `src/components/v2/FeedbackScreen.tsx`**

```tsx
"use client";

/**
 * Форма отзыва о приложении (спека 2026-08-11-feedback-design.md).
 *
 * Оценка обязательна, текст и скриншот - нет. Кнопка неактивна, пока звезда не
 * нажата, и рядом стоит подсказка «Поставьте оценку»: неактивная кнопка без
 * объяснения заставляет человека гадать, что он сделал не так.
 *
 * После отправки показываем отдельный экран благодарности, а не молчаливый
 * возврат назад - человеку важно понять, что его услышали.
 */

import { useState, useRef } from "react";
import { useTranslations } from "next-intl";
import { Link } from "@/i18n/navigation";
import { Headline, Lead } from "@/components/v2/Headline";
import { Button } from "@/components/v2/Button";

const MAX_BODY = 1000;
// Счётчик показываем только ближе к пределу: с первого символа он давит и
// превращает свободный рассказ в упражнение по укладке в лимит.
const COUNTER_FROM = 800;
const MAX_BYTES = 5 * 1024 * 1024;

export function V2FeedbackScreen({ locale }: { locale: string }) {
  const t = useTranslations("Feedback");
  const [rating, setRating] = useState(0);
  const [body, setBody] = useState("");
  const [file, setFile] = useState<File | null>(null);
  const [preview, setPreview] = useState<string | null>(null);
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [done, setDone] = useState<null | { shotFailed: boolean }>(null);
  const fileRef = useRef<HTMLInputElement>(null);

  function pickFile(e: React.ChangeEvent<HTMLInputElement>) {
    const f = e.target.files?.[0] ?? null;
    if (!f) return;
    // Размер проверяем ДО отправки: молчаливый обрыв на десятой секунде
    // загрузки выглядит как поломка приложения, а не как слишком большой файл.
    if (f.size > MAX_BYTES) {
      setError(t("err_too_large"));
      e.target.value = "";
      return;
    }
    setError(null);
    setFile(f);
    if (preview) URL.revokeObjectURL(preview);
    setPreview(URL.createObjectURL(f));
  }

  function removeFile() {
    if (preview) URL.revokeObjectURL(preview);
    setPreview(null);
    setFile(null);
    if (fileRef.current) fileRef.current.value = "";
  }

  async function submit() {
    if (!rating || busy) return;
    setBusy(true);
    setError(null);
    try {
      const fd = new FormData();
      fd.append("rating", String(rating));
      fd.append("body", body);
      fd.append("locale", locale);
      if (file) fd.append("file", file);
      const r = await fetch("/api/feedback", { method: "POST", body: fd });
      const j = (await r.json().catch(() => null)) as
        | { ok: true; screenshot: "saved" | "failed" | "none" }
        | { ok: false; error: string }
        | null;

      if (!j || !j.ok) {
        const code = j && !j.ok ? j.error : "";
        // Введённое НЕ стираем: заставлять человека набирать отзыв заново
        // из-за нашего сбоя - верный способ больше отзывов не получить.
        setError(
          code === "rate_limited"
            ? t("err_rate_limited")
            : code === "too_large"
              ? t("err_too_large")
              : code === "bad_type"
                ? t("err_bad_type")
                : t("err_generic"),
        );
        return;
      }
      setDone({ shotFailed: j.screenshot === "failed" });
    } catch {
      setError(t("err_generic"));
    } finally {
      setBusy(false);
    }
  }

  if (done) {
    return (
      <div
        className="v2-rise"
        style={{
          background: "#fff",
          borderRadius: "16px",
          boxShadow: "var(--v2-shadow-card)",
          padding: "28px 20px",
          textAlign: "center",
        }}
      >
        <div aria-hidden="true" style={{ fontSize: "40px", lineHeight: 1, marginBottom: "12px" }}>
          ★
        </div>
        <Headline size="md" as="h2">
          {t("thanks_title")}
        </Headline>
        <p
          style={{
            marginTop: "10px",
            fontSize: "14px",
            lineHeight: 1.55,
            color: "var(--color-v2-ink-400)",
            fontFamily: "var(--font-v2-body)",
          }}
        >
          {t("thanks_body")}
        </p>
        {done.shotFailed && (
          <p style={{ marginTop: "10px", fontSize: "13px", color: "var(--color-v2-ink-400)" }}>
            {t("shot_failed")}
          </p>
        )}
        <div style={{ marginTop: "20px" }}>
          <Link
            href="/v2/settings"
            style={{
              display: "inline-block",
              padding: "14px 22px",
              borderRadius: "var(--v2-radius-lg)",
              border: "1.5px solid var(--color-v2-ink-500)",
              color: "var(--color-v2-ink-400)",
              textDecoration: "none",
              fontFamily: "var(--font-v2-body)",
              fontSize: "15px",
              fontWeight: 700,
            }}
          >
            {t("thanks_back")}
          </Link>
        </div>
      </div>
    );
  }

  const counterVisible = body.length >= COUNTER_FROM;

  return (
    <div style={{ display: "flex", flexDirection: "column", gap: "16px" }}>
      <div
        className="v2-rise"
        style={{
          background: "#fff",
          borderRadius: "16px",
          boxShadow: "var(--v2-shadow-card)",
          padding: "18px 16px",
        }}
      >
        <p
          style={{
            fontSize: "14px",
            fontWeight: 700,
            marginBottom: "12px",
            fontFamily: "var(--font-v2-body)",
          }}
        >
          {t("rating_label")}
        </p>
        <div role="radiogroup" aria-label={t("rating_label")} style={{ display: "flex", gap: "6px" }}>
          {[1, 2, 3, 4, 5].map((n) => (
            <button
              key={n}
              type="button"
              role="radio"
              aria-checked={rating === n}
              aria-label={String(n)}
              onClick={() => setRating(n)}
              style={{
                flex: 1,
                border: "none",
                background: "transparent",
                cursor: "pointer",
                fontSize: "34px",
                lineHeight: 1.1,
                padding: "4px 0",
                color: n <= rating ? "var(--color-v2-accent, #d4a017)" : "var(--color-v2-ink-500)",
              }}
            >
              ★
            </button>
          ))}
        </div>
        {rating > 0 && (
          <p
            style={{
              marginTop: "8px",
              fontSize: "13px",
              fontWeight: 600,
              color: "var(--color-v2-ink-400)",
              fontFamily: "var(--font-v2-body)",
            }}
          >
            {t(`rating_${rating}` as "rating_1")}
          </p>
        )}
      </div>

      <div
        className="v2-rise"
        style={{
          background: "#fff",
          borderRadius: "16px",
          boxShadow: "var(--v2-shadow-card)",
          padding: "18px 16px",
        }}
      >
        <label
          htmlFor="fb-body"
          style={{
            display: "block",
            fontSize: "14px",
            fontWeight: 700,
            marginBottom: "8px",
            fontFamily: "var(--font-v2-body)",
          }}
        >
          {t("body_label")}
        </label>
        <textarea
          id="fb-body"
          value={body}
          maxLength={MAX_BODY}
          onChange={(e) => setBody(e.target.value)}
          placeholder={t("body_placeholder")}
          rows={5}
          style={{
            width: "100%",
            resize: "vertical",
            padding: "12px",
            borderRadius: "12px",
            border: "1.5px solid var(--color-v2-ink-500)",
            fontFamily: "var(--font-v2-body)",
            fontSize: "15px",
            lineHeight: 1.5,
          }}
        />
        {counterVisible && (
          <p
            aria-live="polite"
            style={{
              marginTop: "6px",
              textAlign: "right",
              fontSize: "12px",
              color: "var(--color-v2-ink-400)",
            }}
          >
            {t("body_counter", { n: body.length, max: MAX_BODY })}
          </p>
        )}
      </div>

      <div
        className="v2-rise"
        style={{
          background: "#fff",
          borderRadius: "16px",
          boxShadow: "var(--v2-shadow-card)",
          padding: "18px 16px",
        }}
      >
        <p
          style={{
            fontSize: "14px",
            fontWeight: 700,
            marginBottom: "8px",
            fontFamily: "var(--font-v2-body)",
          }}
        >
          {t("shot_label")}
        </p>
        {preview ? (
          <div>
            {/* eslint-disable-next-line @next/next/no-img-element -- локальный blob: превью до отправки, next/image здесь не применим */}
            <img
              src={preview}
              alt=""
              style={{ maxWidth: "100%", borderRadius: "12px", display: "block" }}
            />
            <button
              type="button"
              onClick={removeFile}
              style={{
                marginTop: "8px",
                border: "none",
                background: "transparent",
                color: "var(--color-v2-ink-400)",
                fontSize: "14px",
                fontWeight: 600,
                cursor: "pointer",
                padding: "6px 0",
              }}
            >
              {t("shot_remove")}
            </button>
          </div>
        ) : (
          <button
            type="button"
            onClick={() => fileRef.current?.click()}
            style={{
              width: "100%",
              padding: "14px",
              borderRadius: "12px",
              border: "1.5px dashed var(--color-v2-ink-500)",
              background: "#fff",
              color: "var(--color-v2-ink-400)",
              fontFamily: "var(--font-v2-body)",
              fontSize: "15px",
              fontWeight: 600,
              cursor: "pointer",
            }}
          >
            {t("shot_add")}
          </button>
        )}
        <input
          ref={fileRef}
          type="file"
          accept="image/jpeg,image/png,image/webp"
          onChange={pickFile}
          style={{ display: "none" }}
        />
        <p
          style={{
            marginTop: "10px",
            fontSize: "12px",
            lineHeight: 1.5,
            color: "var(--color-v2-ink-400)",
            fontFamily: "var(--font-v2-body)",
          }}
        >
          {t("shot_note")}
        </p>
      </div>

      {error && (
        <p
          role="alert"
          style={{
            fontSize: "13px",
            color: "#b3261e",
            fontFamily: "var(--font-v2-body)",
            lineHeight: 1.5,
          }}
        >
          {error}
        </p>
      )}

      <div>
        <Button onClick={submit} disabled={!rating || busy}>
          {busy ? t("sending") : t("submit")}
        </Button>
        {!rating && (
          <p
            style={{
              marginTop: "8px",
              textAlign: "center",
              fontSize: "13px",
              color: "var(--color-v2-ink-400)",
              fontFamily: "var(--font-v2-body)",
            }}
          >
            {t("rating_hint")}
          </p>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Написать страницу `src/app/[locale]/v2/feedback/page.tsx`**

```tsx
/**
 * Экран «Оставить отзыв» - открывается из настроек рядом с «Пригласить»
 * (см. Settings.feedback_title в SettingsActions.tsx).
 *
 * Гард - requireActiveUser({ allowPaused: true }), как у /v2/invite и
 * /v2/settings: экран висит в настройках, и человек на паузе имеет право
 * сказать, почему он туда ушёл.
 */

import { setRequestLocale, getTranslations } from "next-intl/server";
import { Link } from "@/i18n/navigation";
import { requireActiveUser } from "@/lib/auth/active-guard";
import { getUnreadTotal } from "@/lib/chat/list";
import { BottomNav } from "@/components/bottom-nav";
import { MiniAppShell } from "@/components/v2/MiniAppShell";
import { Headline, Lead } from "@/components/v2/Headline";
import { V2FeedbackScreen } from "@/components/v2/FeedbackScreen";

export const dynamic = "force-dynamic";

export default async function V2FeedbackPage({
  params,
}: {
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  setRequestLocale(locale);
  const t = await getTranslations("Feedback");
  const tSettings = await getTranslations("Settings");
  const user = await requireActiveUser(locale, { allowPaused: true });
  const unread = await getUnreadTotal(user.id);

  return (
    <>
      <MiniAppShell eyebrow={tSettings("title")} align="top" footer={null}>
        <div className="v2-screen-in">
          <div style={{ marginBottom: "12px", marginLeft: "-4px" }}>
            <Link
              href="/v2/settings"
              style={{
                display: "inline-flex",
                alignItems: "center",
                gap: "4px",
                padding: "6px 4px",
                color: "var(--color-v2-ink-400)",
                textDecoration: "none",
                fontFamily: "var(--font-v2-body)",
                fontSize: "14px",
                fontWeight: 600,
              }}
            >
              <span aria-hidden="true" style={{ fontSize: "18px", lineHeight: 1 }}>
                ←
              </span>
              {t("back")}
            </Link>
          </div>

          <Headline size="lg" as="h1">
            {t("title")}
          </Headline>
          <Lead>{t("subtitle")}</Lead>

          <div style={{ marginTop: "28px" }}>
            <V2FeedbackScreen locale={locale} />
          </div>

          <div style={{ height: "80px" }} />
        </div>
      </MiniAppShell>
      <BottomNav active="profile" unread={unread} />
    </>
  );
}
```

- [ ] **Step 3: Проверить типы и сборку**

Run: `cd ~/Code/baxtlilar && pnpm exec tsc --noEmit && pnpm exec next build 2>&1 | tail -25`
Expected: tsc чист; сборка проходит, в списке маршрутов есть `/[locale]/v2/feedback`.

Если `Button` не принимает `disabled` или `Headline` не знает `size="md"` - открыть `src/components/v2/Button.tsx` и `src/components/v2/Headline.tsx` и использовать реально существующие пропсы, а не подгонять компоненты под этот код.

- [ ] **Step 4: Коммит**

```bash
cd ~/Code/baxtlilar && git add src/components/v2/FeedbackScreen.tsx "src/app/[locale]/v2/feedback/page.tsx" && git commit -m "feat(feedback): экран отзыва в мини-аппе"
```

---

### Task 7: Вход на экран из настроек профиля

**Files:**
- Modify: `src/components/v2/SettingsActions.tsx`
- Modify: `messages/{ru,uz,en,tr}.json` (ключи `Settings.feedback_title`, `Settings.feedback_desc`)

**Interfaces:**
- Consumes: маршрут `/v2/feedback` (Task 6)
- Produces: пункт меню в настройках

- [ ] **Step 1: Добавить ключи в namespace `Settings` всех четырёх файлов**

В `messages/ru.json` -> `Settings`:
```json
"feedback_title": "Оставить отзыв",
"feedback_desc": "Расскажите, что работает, а что мешает - это помогает нам чинить нужное."
```
В `messages/uz.json` -> `Settings`:
```json
"feedback_title": "Fikr qoldirish",
"feedback_desc": "Nima ishlayapti va nima xalaqit berayotganini yozing - bu kerakli narsani tuzatishga yordam beradi."
```
В `messages/en.json` -> `Settings`:
```json
"feedback_title": "Leave feedback",
"feedback_desc": "Tell us what works and what gets in the way - it helps us fix the right things."
```
В `messages/tr.json` -> `Settings`:
```json
"feedback_title": "Geri bildirim bırakın",
"feedback_desc": "Neyin işe yaradığını ve neyin engel olduğunu yazın - doğru şeyleri düzeltmemize yardım eder."
```

- [ ] **Step 2: Добавить карточку в `SettingsActions.tsx`**

Сразу после карточки с `href="/v2/invite"` (она заканчивается на `{t('invite_desc')}</p></div>`) вставить точно такую же карточку со ссылкой на отзыв - отдельной карточкой, а не второй ссылкой внутри существующей: это разные действия, и в одном блоке они читались бы как варианты одного:

```tsx
        <div
          className="v2-rise"
          style={{
            background: "#fff",
            borderRadius: "16px",
            boxShadow: "var(--v2-shadow-card)",
            padding: "18px 16px",
          }}
        >
          <Link
            href="/v2/feedback"
            style={{
              display: "block",
              textAlign: "center",
              textDecoration: "none",
              fontFamily: "var(--font-v2-body)",
              fontSize: "16px",
              fontWeight: 700,
              letterSpacing: "-0.01em",
              padding: "17px 24px",
              borderRadius: "var(--v2-radius-lg)",
              border: "1.5px solid var(--color-v2-ink-500)",
              background: "#ffffff",
              color: "var(--color-v2-ink-400)",
            }}
          >
            {t('feedback_title')}
          </Link>
          <p
            style={{
              marginTop: "10px",
              fontSize: "12px",
              color: "var(--color-v2-ink-400)",
              fontFamily: "var(--font-v2-body)",
              lineHeight: "1.5",
            }}
          >
            {t('feedback_desc')}
          </p>
        </div>
```

- [ ] **Step 3: Проверить, что ключи есть во всех локалях и типы целы**

Run:
```bash
cd ~/Code/baxtlilar && node -e "for(const l of ['ru','uz','en','tr']){const s=require('./messages/'+l+'.json').Settings;if(!s.feedback_title||!s.feedback_desc){console.error('нет ключей в',l);process.exit(1)}}console.log('ok')" && pnpm exec tsc --noEmit
```
Expected: `ok`, tsc без ошибок.

- [ ] **Step 4: Коммит**

```bash
cd ~/Code/baxtlilar && git add src/components/v2/SettingsActions.tsx messages/ru.json messages/uz.json messages/en.json messages/tr.json && git commit -m "feat(feedback): пункт «Оставить отзыв» в настройках профиля"
```

---

### Task 8: Раздел «Отзывы» в админке

**Files:**
- Create: `src/lib/admin/load-feedback.ts`
- Create: `src/app/admin/feedback/page.tsx`
- Create: `src/app/admin/feedback/FeedbackList.tsx`
- Modify: `src/lib/admin/permissions.ts` (добавить `"feedback.view"`)
- Modify: `src/components/admin-ops/OpsSidebar.tsx` (пункт меню)

**Interfaces:**
- Consumes: таблица `feedback` (Task 1); `signedFeedbackUrls` (Task 2); `requireAdmin`, `can`, `OpsShell`, `ADMIN` - как в `src/app/admin/invites/page.tsx`
- Produces:
  - `export type FeedbackRow = { id: string; user_id: string; user_label: string | null; rating: number; body: string | null; screenshot_url: string | null; locale: string; created_at: string }`
  - `export async function loadFeedbackRows(): Promise<FeedbackRow[]>`

- [ ] **Step 1: Написать загрузчик `src/lib/admin/load-feedback.ts`**

```ts
import "server-only";
import { supabaseAdmin } from "@/lib/supabase/admin";
import { unwrapRows } from "@/lib/db/unwrap";
import { signedFeedbackUrls } from "@/lib/uploads/storage";

export type FeedbackRow = {
  id: string;
  user_id: string;
  // Человекочитаемое имя автора: имя из Telegram -> @username -> «—».
  user_label: string | null;
  rating: number;
  body: string | null;
  // Уже подписанная ссылка, а не путь: путь без подписи бесполезен, а
  // подписывать в разметке - значит тащить SESSION_SECRET в компонент.
  screenshot_url: string | null;
  locale: string;
  created_at: string;
};

/**
 * Список отзывов для /admin/feedback.
 *
 * Батч вместо N+1: одним select тянем авторов по всем user_id сразу и одним
 * вызовом подписываем все скриншоты - по образцу loadInviteRows.
 *
 * TTL подписи короткий (5 минут по умолчанию signedFeedbackUrls): на
 * скриншоте может оказаться чужая анкета, и ссылка не должна жить дольше
 * просмотра страницы.
 */
export async function loadFeedbackRows(): Promise<FeedbackRow[]> {
  const sb = supabaseAdmin();
  const rows = unwrapRows(
    await sb
      .from("feedback")
      .select("id, user_id, rating, body, screenshot_path, locale, created_at")
      .order("created_at", { ascending: false }),
  );
  if (!rows.length) return [];

  const userIds = [...new Set(rows.map((r) => String(r.user_id)))];
  const users = unwrapRows(
    await sb
      .from("users")
      .select("id, telegram_username, telegram_first_name")
      .in("id", userIds),
  );
  const label = new Map<string, string | null>();
  for (const u of users) {
    const name = (u.telegram_first_name as string | null) ?? null;
    const uname = (u.telegram_username as string | null) ?? null;
    label.set(String(u.id), name ?? (uname ? `@${uname}` : null));
  }

  const urls = await signedFeedbackUrls(rows.map((r) => r.screenshot_path as string | null));

  return rows.map((r) => ({
    id: String(r.id),
    user_id: String(r.user_id),
    user_label: label.get(String(r.user_id)) ?? null,
    rating: Number(r.rating),
    body: (r.body as string | null) ?? null,
    screenshot_url: r.screenshot_path ? (urls[String(r.screenshot_path)] ?? null) : null,
    locale: String(r.locale ?? "ru"),
    created_at: String(r.created_at),
  }));
}
```

- [ ] **Step 2: Добавить право `feedback.view` в `src/lib/admin/permissions.ts`**

В том же списке, где лежит `"invites.manage"` (строка 29), добавить строкой ниже:

```ts
  "feedback.view",
```

Затем найти в этом файле карту «роль -> права» и выдать `feedback.view` тем же ролям, у которых есть `invites.manage`, **плюс** роли модератора: отзывы - это не управление доступом, а обратная связь, её читает тот же человек, что разбирает очередь. Если структура файла другая (например, права перечислены прямо у ролей) - следовать существующей форме, а не переписывать её.

- [ ] **Step 3: Добавить пункт меню в `src/components/admin-ops/OpsSidebar.tsx`**

Рядом со строкой 52 (`/admin/invites`) добавить:

```tsx
      { href: "/admin/feedback", label: "Отзывы", icon: IconStar, perm: "feedback.view" },
```

Если иконки `IconStar` в файле нет - использовать любую уже импортированную (например `IconTicket`) либо добавить новую по образцу существующих в этом же файле. Выдумывать импорт из несуществующего модуля нельзя.

- [ ] **Step 4: Написать клиентский список `src/app/admin/feedback/FeedbackList.tsx`**

```tsx
"use client";

/**
 * Список отзывов: фильтр по оценке и сортировка по свежести.
 *
 * Фильтр и сортировка на клиенте, а не запросом: на закрытом запуске отзывов
 * десятки, лишний round-trip к базе ради переключения радиокнопки не окупается.
 */

import { useState } from "react";
import Link from "next/link";
import { ADMIN } from "@/lib/admin/admin-tokens";
import type { FeedbackRow } from "@/lib/admin/load-feedback";

export function FeedbackList({ rows }: { rows: FeedbackRow[] }) {
  const [minRating, setMinRating] = useState(0);
  const [oldestFirst, setOldestFirst] = useState(false);

  const view = rows
    .filter((r) => (minRating ? r.rating === minRating : true))
    .slice()
    .sort((a, b) =>
      oldestFirst
        ? a.created_at.localeCompare(b.created_at)
        : b.created_at.localeCompare(a.created_at),
    );

  return (
    <div>
      <div style={{ display: "flex", gap: 12, alignItems: "center", marginBottom: 16 }}>
        <label style={{ fontSize: 13, color: ADMIN.ink500 }}>
          Оценка:{" "}
          <select
            value={minRating}
            onChange={(e) => setMinRating(Number(e.target.value))}
            style={{ padding: "4px 8px" }}
          >
            <option value={0}>любая</option>
            {[1, 2, 3, 4, 5].map((n) => (
              <option key={n} value={n}>
                {n}
              </option>
            ))}
          </select>
        </label>
        <button
          type="button"
          onClick={() => setOldestFirst((v) => !v)}
          style={{ padding: "4px 10px", fontSize: 13, cursor: "pointer" }}
        >
          {oldestFirst ? "Сначала новые" : "Сначала старые"}
        </button>
        <span style={{ fontSize: 13, color: ADMIN.ink500 }}>Всего: {view.length}</span>
      </div>

      {view.length === 0 ? (
        <p style={{ color: ADMIN.ink500, fontSize: 14 }}>Отзывов пока нет.</p>
      ) : (
        <div style={{ display: "flex", flexDirection: "column", gap: 12 }}>
          {view.map((r) => (
            <div
              key={r.id}
              style={{
                border: `1px solid ${ADMIN.ink500}33`,
                borderRadius: 10,
                padding: 14,
                display: "flex",
                gap: 14,
              }}
            >
              <div style={{ flex: 1, minWidth: 0 }}>
                <div style={{ display: "flex", gap: 10, alignItems: "baseline", flexWrap: "wrap" }}>
                  <span style={{ fontSize: 16 }} aria-label={`оценка ${r.rating}`}>
                    {"★".repeat(r.rating)}
                    <span style={{ color: `${ADMIN.ink500}66` }}>{"★".repeat(5 - r.rating)}</span>
                  </span>
                  <Link href={`/admin/clients/${r.user_id}`} style={{ fontSize: 13 }}>
                    {r.user_label ?? "—"}
                  </Link>
                  <span style={{ fontSize: 12, color: ADMIN.ink500 }}>
                    {new Date(r.created_at).toLocaleString("ru-RU")} · {r.locale}
                  </span>
                </div>
                <p style={{ marginTop: 8, fontSize: 14, lineHeight: 1.5, whiteSpace: "pre-wrap" }}>
                  {r.body ?? <span style={{ color: ADMIN.ink500 }}>без текста</span>}
                </p>
              </div>
              {r.screenshot_url && (
                <a href={r.screenshot_url} target="_blank" rel="noreferrer">
                  {/* eslint-disable-next-line @next/next/no-img-element -- подписанная ссылка с коротким TTL, next/image её проксировать не должен */}
                  <img
                    src={r.screenshot_url}
                    alt="скриншот из отзыва"
                    style={{ width: 84, height: 84, objectFit: "cover", borderRadius: 8 }}
                  />
                </a>
              )}
            </div>
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 5: Написать страницу `src/app/admin/feedback/page.tsx`**

```tsx
import { notFound } from "next/navigation";
import { requireAdmin } from "@/lib/admin/guard";
import { can } from "@/lib/admin/permissions";
import { supabaseAdmin } from "@/lib/supabase/admin";
import { OpsShell } from "@/components/admin-ops/OpsShell";
import { ADMIN } from "@/lib/admin/admin-tokens";
import { loadFeedbackRows } from "@/lib/admin/load-feedback";
import { FeedbackList } from "./FeedbackList";

export const dynamic = "force-dynamic";

// Раздел «Отзывы» - единственное место, где видны скриншоты из отзывов: на них
// может оказаться чужая анкета, и наружу они не уходят никуда.
export default async function FeedbackPage() {
  const session = await requireAdmin();
  if (!can(session.role, "feedback.view")) notFound();

  const sb = supabaseAdmin();
  const { data: me } = await sb
    .from("admin_users")
    .select("login")
    .eq("id", session.adminId)
    .maybeSingle();

  const rows = await loadFeedbackRows();

  return (
    <OpsShell adminName={me?.login ?? "—"} adminRole={session.role}>
      <h1 style={{ fontSize: 22, fontWeight: 500, marginBottom: 4 }}>Отзывы</h1>
      <p style={{ color: ADMIN.ink500, fontSize: 13, marginBottom: 20 }}>
        Что люди пишут о приложении. Скриншот открывается по подписанной ссылке
        и живёт пять минут - если ссылка перестала открываться, обновите
        страницу.
      </p>
      <FeedbackList rows={rows} />
    </OpsShell>
  );
}
```

- [ ] **Step 6: Проверить типы, тесты и сборку**

Run: `cd ~/Code/baxtlilar && pnpm exec tsc --noEmit && pnpm test 2>&1 | tail -15 && pnpm exec next build 2>&1 | tail -20`
Expected: tsc чист, все тесты зелёные, в списке маршрутов есть `/admin/feedback`.

- [ ] **Step 7: Коммит**

```bash
cd ~/Code/baxtlilar && git add src/lib/admin/load-feedback.ts src/app/admin/feedback/page.tsx src/app/admin/feedback/FeedbackList.tsx src/lib/admin/permissions.ts src/components/admin-ops/OpsSidebar.tsx && git commit -m "feat(feedback): раздел «Отзывы» в админке"
```

---

### Task 9: Витрины аналитики

**Files:**
- Create: `supabase/migrations/20260811180000_feedback_analytics.sql`

**Interfaces:**
- Consumes: таблица `feedback` (Task 1)
- Produces: `analytics.v_feedback_daily`, `analytics.v_feedback_ratings`

- [ ] **Step 1: Написать миграцию**

```sql
-- Витрины по отзывам. Схема analytics существует только на проде (создана
-- вручную), поэтому create schema if not exists - на проде безопасный no-op,
-- локально создаёт пустую схему, иначе интеграционный прогон всех миграций
-- упал бы на "schema analytics does not exist".
create schema if not exists analytics;

-- ⛔ Текстов отзывов здесь НЕТ и быть не должно. Человек может написать в
-- отзыв что угодно, включая чужие имена и обстоятельства, а роль grafana_ro
-- шире круга модераторов. Тексты читают только в /admin/feedback.
create or replace view analytics.v_feedback_daily as
select
  date_trunc('day', created_at)::date as day,
  count(*)                            as feedback_count,
  round(avg(rating)::numeric, 2)      as avg_rating,
  count(*) filter (where screenshot_path is not null) as with_screenshot
from public.feedback
group by 1
order by 1 desc;

-- Распределение оценок: средняя скрывает поляризацию (две единицы и две
-- пятёрки дают ту же тройку, что четыре тройки), а решение «чинить или нет»
-- зависит именно от этого.
create or replace view analytics.v_feedback_ratings as
select rating, count(*) as feedback_count
from public.feedback
group by 1
order by 1;

do $$
begin
  if exists (select 1 from pg_roles where rolname = 'grafana_ro') then
    grant usage on schema analytics to grafana_ro;
    grant select on analytics.v_feedback_daily, analytics.v_feedback_ratings to grafana_ro;
  end if;
end $$;
```

Блок `do $$ ... end $$` нужен потому, что роли `grafana_ro` нет в локальной базе интеграционного прогона - голый `grant` уронил бы весь прогон.

- [ ] **Step 2: Проверить прогон всех миграций**

Run: `cd ~/Code/baxtlilar && bash scripts/test-db/run-integration-tests.sh 2>&1 | tail -20`
Expected: без ошибок.

- [ ] **Step 3: Коммит**

```bash
cd ~/Code/baxtlilar && git add supabase/migrations/20260811180000_feedback_analytics.sql && git commit -m "feat(feedback): витрины аналитики по отзывам без текстов"
```

---

## Выкладка (после всех задач, с явного разрешения владельца)

Порядок обязателен - на этих граблях проект уже стоял:

1. **Миграции на прод, до кода:**
   ```bash
   ssh baxtlilar-vps 'sudo baxtlilar-migrate' # или принятый в проекте способ, см. память baxtlilar_vps
   ```
   Применить `20260811170000_feedback.sql`, затем `20260811180000_feedback_analytics.sql`.
2. **Снять md5 БД до и после** проверочных прогонов - как делали для invite-кодов.
3. **Влить ветку и выложить код.**
4. **Живая проверка:** оставить отзыв без текста и файла, отзыв с текстом, отзыв со скриншотом; четвёртый подряд должен упереться в лимит с понятным текстом. Проверить, что скриншот виден в `/admin/feedback` и НЕ открывается по той же ссылке после истечения TTL.
5. **Убрать тестовые отзывы** из таблицы, если владелец не хочет видеть их в списке.

Отката флагом здесь нет и он не нужен: экран - отдельный маршрут, ничего существующего не трогает. Если что-то пойдёт не так - убрать пункт из настроек одной правкой.

## Самопроверка плана против спеки

| Требование спеки | Где реализовано |
|---|---|
| Экран в Профиль -> «Оставить отзыв» рядом с «Пригласить» | Task 7 |
| Оценка обязательна, 5 звёзд, подпись при выборе | Task 6, Step 1 |
| Текст необязателен, до 1000, счётчик ближе к пределу | Task 6 (`COUNTER_FROM = 800`), Task 1 (CHECK), Task 4 (`body_too_long`) |
| Скриншот необязателен, 1 файл до 5 МБ, предпросмотр и «убрать» | Task 2 (`FEEDBACK_MAX_BYTES`), Task 6 |
| Кнопка активна только при выбранной оценке + подсказка | Task 6, Step 1 |
| Экран благодарности | Task 6, Step 1 |
| Подсказка «не прикладывайте чужие переписки» | Task 5 (`shot_note`), Task 6 |
| Таблица `feedback` со всеми полями | Task 1 |
| Ограничения в БД: rating not null + 1-5, длина body | Task 1 |
| Отдельный бакет + подписанные ссылки | Task 2 |
| Админка «Отзывы»: оценка, текст, миниатюра, имя, дата, ссылка на карточку, сортировка, фильтр | Task 8 |
| Права как у остальных разделов реестра | Task 8, Step 2 |
| Витрины: средняя оценка по дням и число отзывов | Task 9 |
| Тексты в витрины не выносим | Task 9 (явный комментарий, поля не включены) |
| Скриншоты только в админке | Task 8 (единственный потребитель `signedFeedbackUrls`) |
| Лимит 3 в сутки, понятное сообщение | Task 1 (RPC), Task 4 (429), Task 5 (`err_rate_limited`) |
| Оценка вне 1-5 в обход формы - отказ | Task 4 (`bad_rating`), Task 1 (CHECK) |
| Скриншот больше 5 МБ / не тот формат - понятная ошибка до отправки | Task 6 (проверка размера в браузере), Task 4 (400), Task 5 (тексты) |
| Сбой загрузки файла - отзыв не теряется | Task 4 (ветка `upload_failed`), Task 5 (`shot_failed`) |
| Повторная отправка не создаёт дубль | Task 1 (дедуп в RPC), Task 6 (`busy`) |
| Сбой базы - честная ошибка, введённое не стирается | Task 3 (`db_failed`), Task 4 (500), Task 6 (состояние формы не сбрасывается) |
| Четыре языка, обращение на «Вы» | Task 5, Task 7 |
| YAGNI: без ответов из админки, без публичных отзывов, без уведомлений, один скриншот | нигде не реализовано - намеренно |
